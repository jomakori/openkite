apiVersion: v1
kind: Pod
metadata:
  name: crashloop
  namespace: default
spec:
  restartPolicy: Always
  containers:
    - name: crashloop
      image: busybox:1.36
      command: ["sh", "-c", "echo crashing; exit 1"]
