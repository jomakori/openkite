# Used by the test job in lint-test.yml (--profile ci): writes a JUnit
# report that publish-unit-test-result-action renders into the job summary.
# Docs: https://nexte.st/docs/configuration/

[profile.ci]
fail-fast = true

[profile.ci.junit]
path = "junit.xml"
